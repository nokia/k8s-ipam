apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: networkinstances.ipam.alloc.nephio.org
spec:
  group: ipam.alloc.nephio.org
  names:
    categories:
    - nephio
    - ipam
    kind: NetworkInstance
    listKind: NetworkInstanceList
    plural: networkinstances
    singular: networkinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.kind=='Synced')].status
      name: SYNC
      type: string
    - jsonPath: .status.conditions[?(@.kind=='Ready')].status
      name: STATUS
      type: string
    - jsonPath: .metadata.name
      name: NETWORK-INSTANCE
      type: string
    - jsonPath: .spec.prefixes[0].prefix
      name: PREFIX0
      type: string
    - jsonPath: .spec.prefixes[1].prefix
      name: PREFIX1
      type: string
    - jsonPath: .spec.prefixes[2].prefix
      name: PREFIX2
      type: string
    - jsonPath: .spec.prefixes[3].prefix
      name: PREFIX3
      type: string
    - jsonPath: .spec.prefixes[4].prefix
      name: PREFIX4
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkInstance is the Schema for the networkinstances API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NetworkInstanceSpec defines the desired state of NetworkInstance
            properties:
              prefixes:
                description: Prefixes define the aggregate prefixes in the network instance A Network instance needs at least 1 prefix to be defined to become operational
                items:
                  properties:
                    labels:
                      additionalProperties:
                        type: string
                      description: Labels provide metadata to the prefix. They are part of the spec since the allocation selector will use these labels for finer grane selection As such we distinguish clearly between the metadata labels and the labels used in the spec
                      type: object
                    prefix:
                      description: Prefix defines the ip cidr in prefix or address notation. It can be used to define a subnet or specifc addresses
                      pattern: (([0-9]|[1-9][0-9]|1[0-9][0-9]|2[0-4][0-9]|25[0-5])\.){3}([0-9]|[1-9][0-9]|1[0-9][0-9]|2[0-4][0-9]|25[0-5])/(([0-9])|([1-2][0-9])|(3[0-2]))|((:|[0-9a-fA-F]{0,4}):)([0-9a-fA-F]{0,4}:){0,5}((([0-9a-fA-F]{0,4}:)?(:|[0-9a-fA-F]{0,4}))|(((25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])))(/(([0-9])|([0-9]{2})|(1[0-1][0-9])|(12[0-8])))
                      type: string
                  required:
                  - prefix
                  type: object
                type: array
            required:
            - prefixes
            type: object
          status:
            description: NetworkInstanceStatus defines the observed state of NetworkInstance
            properties:
              allocatedPrefixes:
                description: AllocatedPrefixes identifies the prefix that was allocated by the IPAM system from the ni spec
                items:
                  properties:
                    labels:
                      additionalProperties:
                        type: string
                      description: Labels provide metadata to the prefix. They are part of the spec since the allocation selector will use these labels for finer grane selection As such we distinguish clearly between the metadata labels and the labels used in the spec
                      type: object
                    prefix:
                      description: Prefix defines the ip cidr in prefix or address notation. It can be used to define a subnet or specifc addresses
                      pattern: (([0-9]|[1-9][0-9]|1[0-9][0-9]|2[0-4][0-9]|25[0-5])\.){3}([0-9]|[1-9][0-9]|1[0-9][0-9]|2[0-4][0-9]|25[0-5])/(([0-9])|([1-2][0-9])|(3[0-2]))|((:|[0-9a-fA-F]{0,4}):)([0-9a-fA-F]{0,4}:){0,5}((([0-9a-fA-F]{0,4}:)?(:|[0-9a-fA-F]{0,4}))|(((25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])))(/(([0-9])|([0-9]{2})|(1[0-1][0-9])|(12[0-8])))
                      type: string
                  required:
                  - prefix
                  type: object
                type: array
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource
                  properties:
                    kind:
                      description: Type of this condition. At most one of each condition type may apply to a resource at any point in time.
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True, False, or Unknown?
                      type: string
                  required:
                  - kind
                  - lastTransitionTime
                  - reason
                  - status
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
